*.log
.DS_Store
.env
# Ignora qualquer variante (.env.local, .env.testing, .env.dusk.local, ...)
# e mantem apenas o .env.example versionado.
.env.*
!.env.example
.phpactor.json
.phpunit.result.cache
/.fleet
/.idea
/.nova
/.phpunit.cache
/.vscode
/.zed
/auth.json
/node_modules
/public/build
/public/hot
/public/storage
/storage/*.key
/storage/pail
/vendor
Homestead.json
Homestead.yaml
Thumbs.db

# Artefatos de verificacao no navegador (Playwright MCP)
/.playwright-mcp

# Capturas de tela de verificacao. Ja foram commitadas por engano duas vezes;
# imagem que pertence ao produto vive em public/, nao na raiz.
/*.png
